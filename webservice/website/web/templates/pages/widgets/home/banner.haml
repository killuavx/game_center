-load url_tags templatehelper_tags
{% settings_value ENTRY_TYPES as ETS %}
.ban_pic
  %ul.auto.tc
    {% for item in items %}
    {% resource_url_as item 'cover' as cover_url %}
    %li{:style => "background:url(={cover_url}) repeat-x center"}
      %a.auto.width{:href => "{% absolute_url item.content ETS.pc %}",
       :target => "_blank",
       :style => "background:url(={cover_url}) no-repeat center top",
       :title => "={item.title}"}
    {% endfor %}
.cell-bk.tc.rel.num
  %ul
.banner-page
  %a.arrow.prev{:href => "#"}
    %b 上一页
  %a.arrow.next{:href => "#"}
    %b 下一页
.auto.rel.bg
  .bg-t
  .abs.bg-l
  .abs.bg-r
  .abs.bg-c